#!/bin/bash

version: '3.8'

services:
  api_admin:
    image: willymaillot87/pompiers_admin-api:v10
    ports:
      - "8002:8002"
    networks:
      - mynetwork
    env_file:
      - env.txt

  api_user:
    image: willymaillot87/pompiers_user-api:v8
    ports:
      - "8001:8001"
    networks:
      - mynetwork
    env_file:
      - env.txt

networks:
  mynetwork: